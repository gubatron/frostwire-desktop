These jars are here to avoid compilation errors.

For example, gui/ on windows won't compile if the classpath can't find the
com.apple.eawt.* classes. AppleJavaExtensions.jar contains stub implementations
that can be used during compile time.

